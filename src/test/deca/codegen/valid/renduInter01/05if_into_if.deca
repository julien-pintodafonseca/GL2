// Description:
//      Test d'instruction conditionnelle if...else_if imbriquée dans un if...else_if
//      Conditions (variable == entier), (variable > entier) et (variable < entier)
//
// Resultats:
//      x > 0 : 3

{
    int x = 3;
    if ( x > 0 ) {
        println("x > 0 : ", x);
        if ( x == 1)
        {
            println("x = 1 : ", x);
        }
        else if (x == 2) {
            println("x = 2 : ", x);
        }
    } else if ( x < 0) {
        println("x < 0 : ", x);
    }

}
